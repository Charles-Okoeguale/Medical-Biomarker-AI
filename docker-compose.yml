version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "3008:3008"
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://localhost:3008
    ports:
      - "5173:80"
    depends_on:
      - backend